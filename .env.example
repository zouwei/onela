# ============================================
# Onela ORM - 环境变量配置模板
# 复制此文件为 .env 并填入实际值：
#   cp .env.example .env
# ============================================

# === MySQL ===
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=test
MYSQL_PASSWORD=
MYSQL_DATABASE=onela_test

# === PostgreSQL ===
PG_HOST=127.0.0.1
PG_PORT=5432
PG_USER=test
PG_PASSWORD=
PG_DATABASE=onela_test

# === MariaDB ===
MARIADB_HOST=127.0.0.1
MARIADB_PORT=3307
MARIADB_USER=test
MARIADB_PASSWORD=
MARIADB_DATABASE=onela_test

# === SQL Server ===
SQLSERVER_HOST=127.0.0.1
SQLSERVER_PORT=1433
SQLSERVER_USER=sa
SQLSERVER_PASSWORD=
SQLSERVER_DATABASE=onela_test

# === Oracle ===
ORACLE_HOST=127.0.0.1
ORACLE_PORT=1521
ORACLE_USER=test
ORACLE_PASSWORD=
ORACLE_DATABASE=XEPDB1

# === Docker Compose (可选，覆盖 docker-compose.yml 默认值) ===
# MYSQL_ROOT_PASSWORD=changeme
# PG_PASSWORD=changeme
# MARIADB_ROOT_PASSWORD=changeme
# MARIADB_PASSWORD=changeme
# SQLSERVER_SA_PASSWORD=Changeme@123
# ORACLE_SYS_PASSWORD=changeme
# ORACLE_PASSWORD=changeme
